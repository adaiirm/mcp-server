[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP Server with MySQL Database Support"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.3.1",
    "pymysql>=1.1.0",
]
